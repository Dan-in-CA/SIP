Program data:

key['enabled'] = 1 # Bool, Use 1 or 0 to avod difference between Py True and JS true
key['type'] = 'alldays' | 'evendays' | 'odddays' | 'interval'
key['day_mask'] = 127 # days of week 0 to 7, or if type == interval - interval days
key['interval_base_day'] = 4 # based on dse (days since epoc) 
key['start_min'] = 360 # 6AM
key['stop_min'] = 1080 # 6PM
key['cycle_min'] = 240 # 4 hours
key['duration_sec'] = [0]*gv.sd['nst'] # List of duration times of len # stations
key['station_mask'] = [0]*gv.sd['nbrd'] # List of 1 byte per board
key['name'] = 'un-named' # program name

Notes:
I can see how to use the day mask for interval days as a numeric value rather than a mask. 2-255, and the base_day (interval_days) can be used for days remaining.

conversion:
old   new
[0]   ['enabled'] # Bool, 0 or 1
[1]   ['day_mask'] # int days selected to run, also starting in days for interval mode
[2]   ['interval_base_day'] # date interval is based on
[3]   ['start_min'] #  int
[4]   ['stop_min'] # int
[5]   ['cycle_min'] # int
[6]   ['duration_sec'] # list of ints, [0]*gv.sd['nst'] # List of duration times of len # stations
[7]   ['station_mask'] # list of ints [0]*gv.sd['nbrd'] # List of 1 byte per board

Added:
['type'] # string
['name'] # string

Changed:
key['duration_sec'] = [0]*gv.sd['nst'] # List of duration times of len # stations